# Port for the server (default: 3000)
PORT=3000

# Optional: Default cache TTL in seconds (default: 3600 = 1 hour)
CACHE_TTL=3600

# Optional: Maximum items per page (default: 100)
MAX_ITEMS_PER_PAGE=100